// guide-implementation スキル: 実装計画からステップバイステップ実装ガイドを生成

import { WorkerWithSubAgent } from "../../../../app/lib/types";

const guideImplementationSkill = new WorkerWithSubAgent({
  name: "guide-implementation",
  description:
    "承認済みの実装計画からステップバイステップの実装ガイドを生成するスキル。ワークフローの一部として使用される。",
  allowedTools: [
    "Read",
    "Grep",
    "Glob",
    "Write",
    "Task",
    "ToolSearch",
    "mcp__plugin_linear_linear__get_issue",
  ],
  files: [
    { role: "TEMPLATE", filename: "template.md", sortOrder: 1 },
  ],
  agentConfig: {
    model: "inherit",
    tools: ["Read", "Grep", "Glob", "Write", "ToolSearch"],
    content: `承認済みの実装計画を読み、コードベースを調査して、人間向けのステップバイステップ実装ガイドを生成するエージェント。

## 入力

- タスクID
- 実装計画のパス（orchestrator から渡される）

## 出力

- 実装ガイドの保存先パス

## 実行

guide-implementation skill の手順に従って実行する。

- 完成コードは書かない。既存パターンの参照と実装のヒントを提供する
- API名・型名・関数シグネチャなど、調べる手間がかかる具体的な情報は明示する
- コードベースの類似実装を積極的に探し、参考パターンとして提示する

## セキュリティ

セキュアな情報（APIキー、トークン、パスワード、内部URL、個人情報など）は出力に含めないこと。`,
  },
  content: `# Guide Implementation

承認済みの実装計画を読み、コードベースを調査して、人間が実装するためのステップバイステップガイドを生成する。

## 入力

- タスクID（例: \`LIN-123\`, \`quick-add-dark-mode\`）
- plan.md のパス（orchestrator から渡される）

## 手順

### 1. 実装計画の読み込み

入力として渡された plan.md のパスを読み込み、以下を把握する:
- ゴール
- 変更ファイル一覧
- コミット計画
- テスト計画
- 既存パターン

### 2. リポジトリ全体の構造調査

リポジトリを初めて触る人がコードベースを理解できるよう、全体像を調査する。Explore agentを使い以下を把握する:

- トップレベルのディレクトリ構成と各ディレクトリの役割
- アーキテクチャの概要（レイヤー構成、主要モジュール間の関係）
- エントリーポイント（アプリの起動箇所やルーティング定義）
- 主要な設定ファイル（package.json, tsconfig.json, .env.example 等）の場所と役割
- 開発時のコマンド（ビルド、テスト実行、lint等）
- 使用しているフレームワーク・ライブラリの構成

### 3. 変更対象の詳細調査

計画の各変更ファイルについて、Explore agentを使い以下を調査する:

- 変更対象ファイルの現在の内容と構造
- 同じディレクトリ・モジュール内の類似ファイル（パターンの参考になるもの）
- 変更対象の関数・クラスを呼び出している箇所（影響範囲の理解）
- 関連するテストファイルの既存パターン
- import/依存関係
- データの流れ（リクエスト → ハンドラ → サービス → DB 等、変更が関わる処理フロー）

### 4. 実装ガイドの生成

調査結果をもとに、[template.md](template.md) のフォーマットで実装ガイドを生成する。

ガイド生成のルール:

全体構造の説明:
- リポジトリ全体のディレクトリ構成を示し、各ディレクトリが何を担当するか説明する
- 今回の変更に関係するディレクトリ・ファイルと、関係しないディレクトリを明確に区別する
- 「今回触るのはここ、ここは今回は関係ない」を明示する
- データや処理がどう流れるか（リクエストの経路等）を説明し、変更箇所がその流れのどこに位置するかを示す

実装ステップの説明:
- コミット計画の順序に従ってステップを構成する
- 各ファイルの変更について、何をすべきか・なぜそうするかを説明する
- 完成コードそのものは書かない。代わりに既存コードのパターンを参考として示し、それに倣うよう誘導する
- 具体的なAPI名・関数シグネチャ・型名は明示する（調べる手間を省く）
- 「ここを変更してください」ではなく「この関数に〇〇の処理を追加する必要がある。同じファイルの△△関数が同様のパターンで実装されているので参考にする」のように書く
- テストについても、既存テストのパターンを示し、何をテストすべきかを列挙する

### 5. ガイドの保存

\`~/claude-code-data/workflows/{タスクID}/guide.md\` に保存する。

### 6. 結果返却

ガイドの概要に加え、guide.md の保存先パスを返す。`,
});

export default guideImplementationSkill;
