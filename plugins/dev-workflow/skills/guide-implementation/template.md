# 実装ガイド: {タスクID}

## 概要
{ゴール（plan.mdから）}

## 前提知識
{この実装で使う技術・パターンの簡単な説明。初学者が事前に把握しておくべきこと}

---

## リポジトリの全体像

### ディレクトリ構成
```
{プロジェクトルート}/
├── {dir1}/          ← {役割の説明}
├── {dir2}/          ← {役割の説明}
│   ├── {subdir}/    ← {役割の説明}
│   └── {subdir}/    ← {役割の説明}
├── {dir3}/          ← {役割の説明}
└── {config files}   ← {役割の説明}
```

### アーキテクチャ概要
{レイヤー構成やモジュール間の関係を簡潔に説明。例: 「フロントエンド（React）→ APIルート（Next.js）→ サービス層 → DB」}

### 処理の流れ
{今回の変更に関わるデータ/リクエストの流れを説明}
```
{ユーザー操作} → {エントリーポイント} → {処理層} → {データ層}
```

### 今回の作業範囲

触るディレクトリ・ファイル:
- `{path1}` - {何をするか}
- `{path2}` - {何をするか}

今回は関係ないディレクトリ:
- `{path3}` - {何を担当しているか。なぜ今回は不要か}
- `{path4}` - {何を担当しているか。なぜ今回は不要か}

### 開発コマンド
```bash
{ビルドコマンド}     # {説明}
{テストコマンド}     # {説明}
{lint コマンド}      # {説明}
{dev server コマンド} # {説明}
```

---

## Step 1: {コミット1の内容}

このステップのゴール: {何を達成するか}

### {ファイルパス1}（{新規/修正}）

このファイルの役割:
{このファイルがプロジェクト内でどんな位置づけか、何を担当しているか}

変更の目的:
{なぜこのファイルを変更する必要があるか}

やること:
- {具体的なタスク1}
- {具体的なタスク2}

参考パターン:
{同じプロジェクト内の類似コードの場所とその構造の説明。例: 「`src/services/userService.ts` の `getUser` 関数が同様のパターンで実装されている」}

```{lang}
// {ファイル名}:{行番号} 付近の既存コード（参考）
{関連する既存コードの抜粋}
```

ヒント:
- {実装上のヒント1}
- {実装上のヒント2}

### {ファイルパス2}（{新規/修正}）

{同じ構造で記述}

### コミット

ここまでの変更を以下の内容でコミットする:
```
{コミットメッセージ}
```

---

## Step 2: {コミット2の内容}

{同じ構造で記述}

---

## テスト実装ガイド

### テストの全体像
{プロジェクトのテスト構成の説明。テストランナー、テストディレクトリの構造、テストの書き方の慣習}

### {テストファイル1}

テスト対象: {何をテストするか}

テストすべき項目:
- {テストケース1の説明}
- {テストケース2の説明}
- {エッジケースの説明}

参考テストパターン:
```{lang}
// {既存テストファイル名} の類似テスト（参考）
{既存テストの抜粋}
```

---

## 確認チェックリスト

実装完了後に確認すること:
- [ ] すべてのテストが通る
- [ ] 型エラーがない（TypeScriptの場合）
- [ ] 計画にあるすべての変更を実装した
- [ ] 既存パターンに準拠している
- [ ] {プロジェクト固有の確認事項}
